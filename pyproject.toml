[project]
name = "movie-booking"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[dependency-groups]
dev = [
    "anyio>=4.14.2",
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
]
